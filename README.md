# 协同进化（Coevolution）· 1v1 单挑

以物种为单位的 1v1 卡牌对战：每个物种拥有自己的 20 张私有牌组，用三张基本牌决出胜负。
纯前端单机，玩家操作一方，另一方由规则式 AI 驱动。

- 技术栈：Vue 3 + TypeScript + Vite 8 + Tailwind CSS v4
- 规则引擎：纯 TypeScript、零 Vue 依赖、状态可序列化、随机确定性
- 测试：vitest 单元测试（126 个用例，含 200 局 AI 自对局不变式校验）

```bash
npm install
npm run dev     # 开发服务器
npm test        # 运行全部测试
npm run build   # 类型检查 + 生产构建
```

---

## 卡牌

卡牌只有**卡面效果**与**费用**：没有花色，也没有点数——`types.ts` 的 `Card` 只有 `uid` 与 `kind`，
效果与费用都写在 `data/cardDefs.ts`，同名卡牌之间完全等价。

### 三张基本牌

| 牌 | 能量 | 卡面效果 |
|---|---|---|
| **打击** | 1 | 对对方造成 1 点伤害；其可打出【防御】抵消。**没有次数限制**，有多少能量就能打几张。 |
| **防御** | 1 | 抵消一次【打击】。只能在响应时打出。 |
| **回复** | 2 | 回复 1 点体力。出牌阶段只能对自己使用且需已受伤；濒死时可用来自救。 |

费用是**按牌种的固定值**（`data/cardDefs.ts` 的 `cost`）：打击 1 / 防御 1 / 回复 2。

### 私有牌组（每物种 20 张）

| 牌 | 张数 |
|---|---|
| 打击 | 11 |
| 防御 | 6 |
| 回复 | 3 |

牌组是**私有**的：每个物种（即每位玩家）拥有自己的 20 张牌组与自己的弃牌堆，摸牌只从自己的牌组摸，
自己的牌组耗尽时也只把**自己的**弃牌堆洗回自己的牌组，双方互不干扰。
张数比例沿用原先公共牌堆的 30 : 15 : 8，因此攻防节奏基本不变。

暂时 8 个物种共用同一套牌（`data/deck.ts` 的 `SPECIES_DECKS`）。将来要按物种分化牌组时，
把 `BASIC_DECK` 换成各自的序列即可：uid 分配（玩家 0 从 0 起、玩家 1 接续）与守恒校验都由牌组表推导，不需要改。

### 基础规则术语

| 术语 | 含义 |
|---|---|
| 阶段 | 回合被划分成的固定步骤，顺序为准备 → 判定 → 摸牌 → 出牌 → 弃牌 → 结束 |
| 时机 | 引擎在固定时点执行规则效果的位置，如「回合开始时」「阶段开始时」「阶段结束时」 |
| 摸牌数 | 摸牌阶段摸几张牌，默认 2 张；先手角色的第一回合 1 张 |
| 手牌上限 | 弃牌阶段结束时允许保留的手牌数，默认等于当前体力值 |
| 能量 | 使用／打出卡牌要支付的资源，按牌种的固定费用计；**回合开始时回复至上限**（默认 3 点） |
| 上限 | 能量的最大值，由基础值加技能修正得来（【怒吼】+2） |
| 跳过阶段 / 额外的阶段 | 本回合计划中删去某个阶段，或额外插入一个阶段 |

## 对局流程

1. 开局从 8 个物种中随机抽 3 个，玩家选 1 个；AI 从剩余物种中随机选 1 个。
2. 双方各自洗一副 20 张的私有牌组，各摸 4 张起手牌；玩家先手，**先手第一回合少摸 1 张**，此后每回合摸 2 张。
3. 回合流程（六阶段）：
   **回合开始时 → 准备阶段 → 判定阶段 → 摸牌阶段 → 出牌阶段 → 弃牌阶段 → 结束阶段 → 回合结束时**。
   - 每个阶段再分为「阶段开始时 → 阶段进行 → 阶段结束时」，规则效果注册在时机上（`rules/phase.ts`）；引擎**先把游标移到下一个子步骤再产生效果**，所以濒死等结算打断流程后不会重复执行同一个子步骤。
   - 摸牌阶段：默认摸 2 张（先手第一回合 1 张）。
   - 出牌阶段：能量在回合开始时已回满，能付几张【打击】就打几张（没有次数限制）；【回复】仅对自己且需已受伤；可发动主动技。
   - 弃牌阶段：手牌上限 = 当前体力值，超出部分须弃置。
   - 准备 / 判定 / 结束阶段本作没有可结算的内容，保留为空阶段与时机扩展点。
   - 阶段可被**跳过**（`skipPhase`）或插入**额外的阶段**（`addExtraPhase`），作用于本回合剩余的阶段计划 `state.phaseQueue`。
4. **单将决胜**：任意一方体力归零即败。

### 结算链（完备性说明）

- **使用【打击】**：合法性校验（牌面选项 + **能量是否够付**）→ 支付能量 → 指定目标 → 询问目标打出【防御】（【威压】为 2 张，逐张询问）→ 抵消或造成 1 点伤害 → 结算收尾。
- **伤害结算顺序**：`damage.before`（预留钩子）→ 扣减体力 → 写入伤害上下文 → 「受到伤害后」技能（【夺食】／【狡计】，人类玩家可选发动）→ 濒死检查。
  造成伤害的牌在整条链走完之前一直留在**处理区**，这正是【夺食】能把牌取回手牌的原因；结算完全结束后才进入弃牌堆。
- **处理区带归属**：一次【打击】的收尾要同时处理攻方的打击牌与守方的防御牌，而弃牌堆是私有的，因此处理区每个条目都记录 `owner`，收尾时**分别**进入各自的弃牌堆。
- **濒死**：从濒死者开始按座次询问所有存活角色是否使用【回复】；可连续使用直到体力回到 1 以上；队列耗尽则死亡。**濒死中的【回复】不受「仅自己／已受伤／出牌阶段」限制，但仍然要付 2 点能量**。
- **死亡**：标记阵亡 → 弃置全部手牌（进自己的弃牌堆）→ 清空结算栈与处理区（按条目归属分流）→ 判定胜负。
- **「失去体力」≠「受到伤害」**：【透支】的体力流失不会触发【夺食】／【狡计】，但同样会进入濒死。
- **牌数守恒**：任意结算步之后，双方**合计 40 张**牌各自恰好属于「自己的牌组／手牌／自己的弃牌堆／处理区」之一，且 uid 全局唯一，引擎在每次 `submit` 后自动校验。
  口径是**全局**而非「每方恒为 20 张」：【夺食】会把对手的牌拿进自己手里，之后再弃置就归获得者的弃牌堆，双方池子的张数会此消彼长，但总额不变。
- **牌组耗尽**：某方把自己的弃牌堆洗回自己的牌组；两者同时为空则跳过摸牌并记录日志。

结算顺序由显式的**结算帧栈**驱动（`strike → damage → dying → flush`），而不是散落的回调，因此顺序可测、可读、可扩展。

### 能量系统

| 规则 | 内容 |
|---|---|
| 上限 | 每人 3 点；【怒吼】+2（熊 5 点） |
| 回复 | **回合开始时回复至上限**，只回复回合角色的能量；没花完的余额留在池中（不超过上限） |
| 付费范围 | **所有使用与打出**：出牌阶段使用、响应【打击】打出【防御】、濒死使用【回复】（救援者付自己的能量） |
| 费用 | 按「**当作的牌面**」计：打击 1 / 防御 1 / 回复 2 |
| 不付费 | 主动技（疗愈／透支）、弃牌阶段的弃置、疗愈的弃牌 |

三个直接推论：

1. **防御要预留能量**。能量只在自己回合开始时回满，所以拦截对手【打击】用的是你上回合没花完的部分——「这回合多打两张」与「留 1 点防守」是一对取舍。AI 也按这条行事（手上有能打出的【防御】时留 1 点）。
2. **濒死自救要付 2 点能量**。把能量打光之后被打进濒死，就可能没有能量自救而阵亡。
3. **【打击】的次数限制由能量接管**。同一回合可以连打多张【打击】，上限是能量而不是次数；相应地，**任何牌面的费用都不允许为 0**（0 费 + 无次数限制 = 无限连击，`energy.test.ts` 对此有断言）。

能量与「牌数守恒」一样是硬不变式：任意结算步之后必须落在 `0..上限` 之内，引擎在每次 `submit` 后自动校验（`assertEnergyBounds`）。

参数在 `src/game/rules/energy.ts`（`BASE_ENERGY_MAX`）与 `src/game/data/cardDefs.ts`（每张牌的 `cost`）。

### 消耗战（终止规则，重要）

三张基本牌的 1v1 存在天然的僵持：鹿每回合回 1 点刚好抵消每回合 1 点伤害，【疾影】可以把每次【打击】都转成【防御】挡掉，而牌组会无限洗回，于是对局可以永远打下去。能量系统让「双方都攒着能量不进攻」成为另一种僵持：谁先动手谁吃亏。

因此从**第 21 回合**起进入消耗战：每个回合的「回合开始时」时机，回合角色失去体力，每 5 回合递增 1 点（`1 + ⌊(回合 − 21) / 5⌋`）。流失量无上限增长，最终必定超过任何回复能力，保证对局必然结束。能量的回满发生在这条效果之前，所以因消耗战进入濒死的角色是带着满能量被询问的。

消耗战不是写在流程里的特例，而是注册在「回合开始时」时机上的规则效果（`TURN_TIMING_EFFECTS`），与将来的判定类效果、时机类技能走同一条通路。

参数在 `src/game/rules/turn.ts`（`ATTRITION_TURN` / `ATTRITION_STEP`）。

## 物种与技能

| 物种 | 体力 | 技能 | 类型 | 效果 |
|---|---|---|---|---|
| 🐯 虎 | 4 | —— | —— | 暂时没有技能：原【猛扑】以「红色牌」为判定依据，卡牌移除花色后一并移除 |
| 🐻 熊 | 4 | **怒吼** | 常驻 | 你每回合的能量上限 +2（3 → 5） |
| 🐆 豹 | 4 | **疾影** | 转化 | 你可以将【打击】当【防御】、【防御】当【打击】使用或打出 |
| 🐺 狼 | 4 | **夺食** | 触发 | 当你受到伤害后，你可以获得造成此伤害的牌 |
| 🦌 鹿 | 3 | **疗愈** | 主动 | 出牌阶段限一次，弃一张手牌，令一名已受伤的角色回复 1 点体力 |
| 🦁 狮 | 4 | **威压** | 常驻 | 你使用【打击】时，目标需依次打出两张【防御】才能抵消 |
| 🐂 牛 | 4 | **透支** | 主动 | 出牌阶段，你可以失去 1 点体力，然后摸两张牌 |
| 🦊 狐 | 3 | **狡计** | 触发 | 当你受到伤害后，你可以获得伤害来源的一张手牌 |

原【猛扑】（虎）与【灵草】（鹿）都靠「红色牌」判定，卡牌去掉花色后已无依据，因此**整条移除**
（`SkillId`、技能表、`useOptions`、AI 决策与相关测试一并删除）；鹿保留【疗愈】，豹的【疾影】按牌种转化，不受影响。
重新设计出不含花色的效果后再把技能登记回 `IMPLEMENTED_SKILLS` 即可。

转化型技能产生的牌统一用 `VirtualCard` 表示（`as` = 当作什么牌，`source` = 实际消耗的牌，`via` = 技能），响应、伤害、日志、弃牌全部只认它，避免结算与展示分叉。

## 目录结构

```
src/
  game/                    # 规则引擎（纯 TS，不依赖 Vue）
    types.ts               # 领域类型：Card / VirtualCard / Prompt / Action / Frame / Phase / GameState
    engine.ts              # createGame / rollDraft / getPending / submit / advance
    rng.ts                 # mulberry32 + 种子化洗牌（可复现）
    log.ts                 # 中文战报
    testUtils.ts           # 测试用状态构造器
    data/                  # 私有牌组表（每物种 20 张）、物种表、牌定义
    rules/                 # phase（六阶段、时机、跳过/额外阶段）
                           # turn（回合推进、摸牌数、手牌上限、消耗战）
                           # energy（能量消耗、回满、能量不变式）
                           # usage（使用次数统计与「每回合限一次」技能记录）
                           # legality / damage / dying / death / respond / cardZones / distance
    skills/                # index（转化与常驻） / effects（主动与触发）
    ai/                    # 规则式 AI（确定性）
  stores/game.ts           # reactive 状态 + AI 驱动循环（pumpToken 防竞态）
  components/              # StartScreen / DraftScreen / GameBoard / PlayerPanel / HandCard
                           # ActionBar / PromptOverlay / LogPanel / HealthBar
```

引擎以 **prompt 驱动的状态机**对外：`submit(state, action)` 先校验再应用，然后 `advance()` 自动推进系统步骤，直到停在需要人或 AI 决策的点上。回合推进由 `(阶段, 子步骤)` 游标加剩余阶段队列 `phaseQueue` 表示，规则效果按「时机」注册。界面用 `reactive()` 包裹同一份状态对象，引擎就地修改它即自动刷新，引擎本身永远不 import Vue。

## 测试覆盖（126 个用例）

| 文件 | 覆盖内容 |
|---|---|
| `data/deck.test.ts` | 每物种 20 张、11/6/3 分类、卡牌只有 uid 与牌种、uid 从 uidBase 连续、双方 uid 不重叠、各物种暂时同套、洗牌可复现 |
| `rng.test.ts` | 同种子同序列、分布均匀、洗牌不增删牌 |
| `rules/phase.test.ts` | 六阶段顺序、阶段前后时机、跳过/额外阶段、被濒死打断后不重复执行时机 |
| `rules/turn.test.ts` | 摸牌数与先手补偿、手牌上限与弃牌数、自己的弃牌堆洗回自己的牌组、双耗尽跳过、消耗战递增与时机顺序、回合开始回满能量 |
| `rules/energy.test.ts` | 费用表（1/1/2）与牌面文案一致、费用恒 ≥ 1、上限与怒吼 +2、开局回满、使用／响应／回复扣费、转化牌按当作的牌面付费、能量不足拒绝且状态不变、0 能量无可用牌面、技能不耗能、只有回合角色回满 |
| `rules/usage.test.ts` | 按牌名分角色计数、转化牌按当作的牌名计数、技能每回合限一次、回合开始重置 |
| `rules/strike.test.ts` | 抵消／伤害、双方弃牌各自归位、【打击】无次数限制（受能量约束）、能量耗尽后禁用、怒吼、威压两张、满血禁用回复、非法动作不改状态 |
| `rules/damage.test.ts` | 扣体力、时机顺序（扣血→夺食→濒死）、夺食取回处理区的牌、狡计、无手牌不询问 |
| `rules/dying.test.ts` | 自救、体力为负需连用多张、双方放弃即死亡、救援不受限制、濒死中禁用打击、能量不足无法自救／救援 |
| `rules/death.test.ts` | 弃置全部手牌、处理区按归属收尾、终局后拒绝动作 |
| `skills/transform.test.ts` | 疾影双向、不能转化【回复】、不可冒用、虎/鹿暂时没有转化技 |
| `skills/active.test.ts` | 透支非伤害且不触发夺食、透支濒死后才摸牌、疗愈限一次与目标校验 |
| `skills/passive.test.ts` | 怒吼能量上限 +2、威压 need=2、物种名与技能名不重复、技能集合与已实现技能完全一致、虎暂时无技能 |
| `engine.test.ts` | **200 局 AI 自对局**全终局且牌数守恒、能量不变式、8×8 物种组合、完全确定性复现、回归：曾经的死循环组合；私有牌组：开局各 20 张且构成正确、摸牌不影响对手、夺食转移归属后全局仍守恒 |
| `stores/game.test.ts` | 抽将→选将→对局→终局全链路、只提供合法操作、出牌扣能量、能量见底只能结束阶段、弃牌校验、再来一局不被旧回调污染（固定种子） |

## 已知简化

- **卡牌没有花色与点数**：`Card` 只有 `uid` 与 `kind`，费用与效果都按牌种固定；由此**依赖花色的技能一并移除**（虎的【猛扑】、鹿的【灵草】），虎暂时没有技能。
- **无随机判定**：没有任何需要翻牌判定的机制，技能只依赖牌种与手牌数（唯一的随机是【狡计】取哪张手牌）。
- **能量不跨回合累积**：没有「存能量」的概念，回合开始时只是回复至上限，多出来的部分不会保留。
- **技能不消耗能量**：【疗愈】【透支】以及弃牌都不付能量，能量只约束「使用／打出卡牌」。
- **濒死可能因能量不足而救不回来**：濒死【回复】照常收 2 点能量，这是「所有使用与打出都消耗」的直接结果。
- **判定 / 准备 / 结束阶段为空**：本作没有需要判定的内容，这三个阶段保留为空阶段与时机扩展点。
- **无额外回合**：阶段层面支持「跳过阶段 / 额外的阶段」，但还没有「额外回合」；将来加入时挂在「回合结束时」的时机上。
- **牌池只有三张基本牌**：没有装备牌，也没有额外的策略牌，因此不存在多目标牌与对应的响应链。
- **牌组私有但张数会漂移**：【夺食】把对手的牌拿进自己手里后，那张牌就归获得者所有（弃置 / 洗回都进获得者的牌区），因此双方池子不再各为 20 张，守恒口径是「全局 40 张、uid 唯一」。
- **所有物种暂时共用同一套牌**：`SPECIES_DECKS` 指向同一个 `BASIC_DECK`，按物种分化牌组只需替换各自序列。
- **距离恒为 1**：1v1 双方座位距离固定，`rules/distance.ts` 保留完整接口以便扩展多人。
- **濒死询问顺序**：从濒死者开始按座次询问（1v1 即濒死者 → 对手），`util.ts` 的 `aliveOrderFrom` 是顺序扩展点。
- **【疗愈】目标**：可以指定任意已受伤角色（规则上允许治疗对手，AI 永远不会这么做）。
- **【狡计】**：从伤害来源手牌中随机获得一张。
- **AI 是规则式的**：付得起就出【防御】、濒死不救对手、弃牌按「打击→防御→回复」优先级；进攻时手上有能打出的【防御】就留 1 点能量防守。它不会做长线规划。

### 平衡观察

用 200 局 AI 自对局统计（牌组私有化 + 移除花色之后重测）：

| 指标 | 数值 |
|---|---|
| 中位局长 | 14 回合 |
| 平均局长 / 最短 / 最长 | 14.4 / 1 / 29 回合 |
| 打到消耗战（≥ 21 回合）的比例 | 34% |
| 平均每局【打击】次数 | 17.0 |
| 先手胜率 | 49.5% |
| 物种胜场（200 局） | 狮 43 > 狐 34 > 鹿 32 > 熊 27 > 狼 25 > 豹 18 > 虎 13 > 牛 8 |

私有牌组把「双方共享 53 张」换成「各 20 张、互不干扰」之后，局长与消耗战比例几乎不变（12 → 14 回合、33% → 34%）：
每方 20 张的循环速度与原公共牌堆接近，而【回复】从全局 8 张变成每方 3 张，回复总量略降，所以方差依旧存在
（最短的一局仍在第 1 回合就被满能量的熊连击打死）。

**狮最强**是能量系统的直接后果：【威压】要求目标连续打出两张【防御】，在能量稀缺时等于把防守成本翻倍，对手往往只能选择挨打。

**虎与牛垫底**与本次改动直接相关：虎失去了唯一的技能（【猛扑】依赖花色而被移除），牛只有【透支】而手牌循环变快后收益有限。
重新为虎设计一个不依赖花色的技能，是下一步最直接的平衡动作。

可调旋钮（改完记得同步测试期望值）：

- `src/game/data/deck.ts`：每个物种私有牌组的构成（`DECK_TABLE`，默认 11/6/3）——调高【回复】会让消耗战更常见。
- `src/game/rules/energy.ts`：`BASE_ENERGY_MAX`（默认 3）——调高会整体加快节奏。
- `src/game/data/cardDefs.ts`：每张牌的 `cost`（默认 1/1/2）。
- `src/game/skills/index.ts`：`ROAR_ENERGY_BONUS`（怒吼给熊的 +2）。
- `src/game/ai/index.ts`：`DEFENSE_RESERVE`（AI 留多少能量防守，默认 1）。
- `src/game/data/species.ts`：调整物种体力上限。
- `src/game/rules/turn.ts`：调整消耗战起始回合与递增步长。
