# 协同进化（Coevolution）· 1v1 单挑

以物种为单位的 1v1 卡牌对战：八个物种各有一项技能，用三张基本牌决出胜负。
纯前端单机，玩家操作一方，另一方由规则式 AI 驱动。

- 技术栈：Vue 3 + TypeScript + Vite 8 + Tailwind CSS v4
- 规则引擎：纯 TypeScript、零 Vue 依赖、状态可序列化、随机确定性
- 测试：vitest 单元测试（93 个用例，含 200 局 AI 自对局不变式校验）

```bash
npm install
npm run dev     # 开发服务器
npm test        # 运行全部测试
npm run build   # 类型检查 + 生产构建
```

---

## 卡牌

### 三张基本牌

| 牌 | 作用 |
|---|---|
| **打击** | 对对方造成 1 点伤害；其可打出【防御】抵消。每回合限用一次。 |
| **防御** | 抵消一次【打击】。只能在响应时打出，无法主动使用。 |
| **回复** | 回复 1 点体力。出牌阶段只能对自己使用且需已受伤；濒死时可用来自救。 |

### 牌堆（53 张）

| 牌 | 张数 | 花色分布 |
|---|---|---|
| 打击 | 30 | ♠10 + ♣13（黑）／♥3 + ♦4（红） |
| 防御 | 15 | ♦10 + ♥5（全红） |
| 回复 | 8 | ♥7 + ♦1（全红） |

合计红 30 / 黑 23。牌张保留花色与点数（【猛扑】【灵草】需要判断红色）；
**点数仅用于展示**，本作没有任何随机判定机制，点数不参与结算。

防御与回复全红是刻意设计：它们是转化型技能唯一的牌源，全红可保证红牌占到 30/53。

## 对局流程

1. 开局从 8 个物种中随机抽 3 个，玩家选 1 个；AI 从剩余物种中随机选 1 个。
2. 双方各摸 4 张起手牌；玩家先手，**先手第一回合少摸 1 张**，此后每回合摸 2 张。
3. 回合流程：回合开始 → 摸牌 → 出牌 → 弃牌 → 回合结束。
   - 出牌阶段：【打击】每回合限一次（【怒吼】除外）；【回复】仅对自己且需已受伤；可发动主动技。
   - 弃牌阶段：**手牌上限 = 当前体力值**，超出部分弃置。
4. **单将决胜**：任意一方体力归零即败。

### 结算链（完备性说明）

- **使用【打击】**：合法性校验 → 指定目标 → 询问目标打出【防御】（【威压】为 2 张，逐张询问）→ 抵消或造成 1 点伤害 → 结算收尾。
- **伤害结算顺序**：`damage.before`（预留钩子）→ 扣减体力 → 写入伤害上下文 → 「受到伤害后」技能（【夺食】／【狡计】，人类玩家可选发动）→ 濒死检查。
  造成伤害的牌在整条链走完之前一直留在**处理区**，这正是【夺食】能把牌取回手牌的原因；结算完全结束后才进入弃牌堆。
- **濒死**：从濒死者开始按座次询问所有存活角色是否使用【回复】；可连续使用直到体力回到 1 以上；队列耗尽则死亡。**濒死中的【回复】不受「仅自己／已受伤／出牌阶段」限制**。
- **死亡**：标记阵亡 → 弃置全部手牌 → 清空结算栈与处理区 → 判定胜负。
- **「失去体力」≠「受到伤害」**：【透支】的体力流失不会触发【夺食】／【狡计】，但同样会进入濒死。
- **牌区守恒**：任意结算步之后，全 53 张牌各自恰好属于「牌堆／手牌／弃牌堆／处理区」之一，引擎在每次 `submit` 后自动校验。
- **牌堆耗尽**：弃牌堆洗回牌堆；两者同时为空则跳过摸牌并记录日志。

结算顺序由显式的**结算帧栈**驱动（`strike → damage → dying → flush`），而不是散落的回调，因此顺序可测、可读、可扩展。

### 消耗战（终止规则，重要）

三张基本牌的 1v1 存在天然的僵持：鹿每回合回 1 点刚好抵消每回合 1 点伤害，【疾影】可以把每次【打击】都转成【防御】挡掉，而牌堆会无限洗回，于是对局可以永远打下去。

因此从**第 21 回合**起进入消耗战：每个回合开始时，回合角色失去体力，每 5 回合递增 1 点（`1 + ⌊(回合 − 21) / 5⌋`）。流失量无上限增长，最终必定超过任何回复能力，保证对局必然结束。

参数在 `src/game/rules/turn.ts`（`ATTRITION_TURN` / `ATTRITION_STEP`）。

## 物种与技能

| 物种 | 体力 | 技能 | 类型 | 效果 |
|---|---|---|---|---|
| 🐯 虎 | 4 | **猛扑** | 转化 | 你可以将一张红色牌当【打击】使用或打出 |
| 🐻 熊 | 4 | **怒吼** | 常驻 | 你使用【打击】无次数限制 |
| 🐆 豹 | 4 | **疾影** | 转化 | 你可以将【打击】当【防御】、【防御】当【打击】使用或打出 |
| 🐺 狼 | 4 | **夺食** | 触发 | 当你受到伤害后，你可以获得造成此伤害的牌 |
| 🦌 鹿 | 3 | **灵草** | 转化 | 你的回合外，你可以将一张红色牌当【回复】使用 |
| 🦌 鹿 | 3 | **疗愈** | 主动 | 出牌阶段限一次，弃一张手牌，令一名已受伤的角色回复 1 点体力 |
| 🦁 狮 | 4 | **威压** | 常驻 | 你使用【打击】时，目标需依次打出两张【防御】才能抵消 |
| 🐂 牛 | 4 | **透支** | 主动 | 出牌阶段，你可以失去 1 点体力，然后摸两张牌 |
| 🦊 狐 | 3 | **狡计** | 触发 | 当你受到伤害后，你可以获得伤害来源的一张手牌 |

转化型技能产生的牌统一用 `VirtualCard` 表示（`as` = 当作什么牌，`source` = 实际消耗的牌，`via` = 技能），响应、伤害、日志、弃牌全部只认它，避免结算与展示分叉。

## 目录结构

```
src/
  game/                    # 规则引擎（纯 TS，不依赖 Vue）
    types.ts               # 领域类型：Card / VirtualCard / Prompt / Action / Frame / GameState
    engine.ts              # createGame / rollDraft / getPending / submit / advance
    rng.ts                 # mulberry32 + 种子化洗牌（可复现）
    log.ts                 # 中文战报
    testUtils.ts           # 测试用状态构造器
    data/                  # 牌堆表、物种表、牌定义
    rules/                 # legality / damage / dying / death / respond / turn / cardZones / distance
    skills/                # index（转化与常驻） / effects（主动与触发）
    ai/                    # 规则式 AI（确定性）
  stores/game.ts           # reactive 状态 + AI 驱动循环（pumpToken 防竞态）
  components/              # StartScreen / DraftScreen / GameBoard / PlayerPanel / HandCard
                           # ActionBar / PromptOverlay / LogPanel / HealthBar
```

引擎以 **prompt 驱动的状态机**对外：`submit(state, action)` 先校验再应用，然后 `advance()` 自动推进系统步骤，直到停在需要人或 AI 决策的点上。界面用 `reactive()` 包裹同一份状态对象，引擎就地修改它即自动刷新，引擎本身永远不 import Vue。

## 测试覆盖（93 个用例）

| 文件 | 覆盖内容 |
|---|---|
| `data/deck.test.ts` | 53 张、30/15/8 分类、uid 唯一、红黑比、洗牌可复现 |
| `rng.test.ts` | 同种子同序列、分布均匀、洗牌不增删牌 |
| `rules/turn.test.ts` | 摸牌与先手补偿、弃牌上限、牌堆洗回、双耗尽跳过、消耗战递增与不重复扣体力 |
| `rules/strike.test.ts` | 抵消／伤害、每回合限一次、怒吼、威压两张、满血禁用回复、非法动作不改状态 |
| `rules/damage.test.ts` | 扣体力、时机顺序（扣血→夺食→濒死）、夺食取回处理区的牌、狡计、无手牌不询问 |
| `rules/dying.test.ts` | 自救、体力为负需连用多张、双方放弃即死亡、救援不受限制、濒死中禁用打击 |
| `rules/death.test.ts` | 弃置全部手牌、处理区收尾、终局后拒绝动作 |
| `skills/transform.test.ts` | 猛扑（红牌限定、不能产出防御、不可冒用）、疾影双向、灵草回合外限定 |
| `skills/active.test.ts` | 透支非伤害且不触发夺食、透支濒死后才摸牌、疗愈限一次与目标校验 |
| `skills/passive.test.ts` | 怒吼解除次数限制、威压 need=2、物种名与技能名不重复、技能集合与已实现技能完全一致 |
| `engine.test.ts` | **200 局 AI 自对局**全终局且牌数守恒、8×8 物种组合、完全确定性复现、回归：曾经的死循环组合 |
| `stores/game.test.ts` | 抽将→选将→对局→终局全链路、只提供合法操作、弃牌校验、再来一局不被旧回调污染 |

## 已知简化

- **无随机判定**：没有任何需要翻牌判定的机制，技能只依赖花色与手牌数；点数仅用于展示。
- **牌池只有三张基本牌**：没有装备牌，也没有额外的策略牌，因此不存在多目标牌与对应的响应链。
- **距离恒为 1**：1v1 双方座位距离固定，`rules/distance.ts` 保留完整接口以便扩展多人。
- **【疗愈】目标**：可以指定任意已受伤角色（规则上允许治疗对手，AI 永远不会这么做）。
- **【狡计】**：从伤害来源手牌中随机获得一张。
- **AI 是规则式的**：总是响应【防御】、濒死不救对手、弃牌按「黑打击→红打击→防御→回复」优先级。它不会做长线规划。

### 平衡观察

用 200 局 AI 自对局统计：中位局长约 23 回合，其中约 2/3 的对局会打到消耗战（第 21 回合）之后。也就是说在这套三张基本牌的配置下，纯靠战斗分胜负偏慢，消耗战压力是实际决定胜负的主要因素之一——这是「只有打击/防御/回复」这一牌池的固有特性（每回合 1 点伤害很容易被 1 张防御或 1 点回复抵消）。

若想让战斗本身更快分胜负，旋钮有三个：

- `src/game/data/deck.ts`：调整打击／防御／回复的比例（例如降低防御与回复占比），改完记得同步 `deck.test.ts` 的期望值。
- `src/game/data/species.ts`：调整物种体力上限。
- `src/game/rules/turn.ts`：调整消耗战起始回合与递增步长。
