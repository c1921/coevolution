# 协同进化（Coevolution）· 1v1 单挑

以代号为单位的 1v1 卡牌对战：每个代号拥有自己的 20 张私有牌组，用五张牌（打击 / 防御 / 回复 / 急救 / 风暴）决出胜负。
纯前端单机，玩家操作一方，另一方由规则式 AI 驱动。

- 技术栈：Vue 3 + TypeScript + Vite 8 + Tailwind CSS v4
- 规则引擎：纯 TypeScript、零 Vue 依赖、状态可序列化、随机确定性
- 测试：vitest 单元测试（316 个用例，含 200 局 AI 自对局不变式校验）

```bash
npm install
npm run dev     # 开发服务器
npm test        # 运行全部测试
npm run build   # 类型检查 + 生产构建
```

---

## 卡牌

卡牌只有**卡面效果**与**费用**：没有花色，也没有点数——`types.ts` 的 `Card` 只有 `uid` 与 `kind`，
效果与费用写在 `data/cardDefs.ts`，同名卡牌之间完全等价。

### 威胁：本作唯一的基础伤害机制

攻击**不直接扣血**，而是给目标叠加**威胁**：

1. A 的回合，A 攻击 B → **B 获得威胁**（体力不变）。
2. B 的回合，B 可以打出【防御】抵消自己的威胁（1 张抵消 1 点）。
3. **B 的回合结束时**，B 剩余的威胁结算为等量伤害（走伤害帧：「受到伤害后」技能 → 濒死检查），随后归零。

威胁**不跨回合累积**：它只在「施加者的回合」与「承受者回合结束」之间存活一轮，因此每个威胁都恰好给承受者一次应对机会。
所有攻击类内容（【打击】/【风暴】/【强袭】）都只经由 `threat` 指令走这一条路径；
唯一的例外是「失去体力」（`lose-hp`：技能的自伤与【消耗战】的流失），它不是伤害，直接扣血、不触发「受到伤害后」技能。

### 五张牌

| 牌 | 能量 | 卡面效果 |
|---|---|---|
| **打击** | 1 | 令对方获得 1 点威胁（数值走 `threat-per-attack` 通道，可由技能修正）。**没有次数限制**，有多少能量就能打几张。 |
| **防御** | 1 | **在自己的出牌阶段**抵消自己 1 点威胁（自己至少有 1 点威胁时才能使用）。 |
| **回复** | 2 | 回复 1 点体力。出牌阶段只能对自己使用且需已受伤；濒死时可用来自救。 |
| **急救** | 2 | **使用时选择目标**：令一名已受伤的角色（可以是自己）回复 1 点体力。 |
| **风暴** | 2 | **多目标**：令每名角色（包括你自己）获得 2 点威胁。 |

费用是**按牌种的固定值**（`data/cardDefs.ts` 的 `cost`）：打击 1 / 防御 1 / 回复 2 / 急救 2 / 风暴 2。

【打击】与【防御】是同一套威胁机制的两端；【急救】与【风暴】分别验收了两条机制：**使用卡牌时选目标**（`UseVariant.target`，
与主动技共用 `cardTargetChoice`）与**多目标**（`TargetSpec.count` + `for-each-target`）。
这些牌都只是 JSON，引擎、legality、AI 与界面里没有任何牌种分支。

### 私有牌组（每代号 20 张，三套）

| 牌组 | 构成 | 代号 |
|---|---|---|
| `basic` | 打击 11 / 防御 6 / 回复 3 | 反击型 |
| `aggressive` | 打击 11 / 防御 5 / 回复 2 / 急救 1 / 风暴 1 | 进攻型、转化型 |
| `guarded` | 打击 10 / 防御 7 / 回复 3 | 防御型 |

牌组是**私有**的：每个代号（即每位玩家）拥有自己的 20 张牌组与自己的弃牌堆，摸牌只从自己的牌组摸，
自己的牌组耗尽时也只把**自己的**弃牌堆洗回自己的牌组，双方互不干扰。

按代号分化牌组只需新增/替换一份牌组 JSON 并改代号文档的 `deck` 字段：uid 分配（玩家 0 从 0 起、玩家 1 接续）与守恒校验都由牌组文档推导，不需要改代码。
三套牌组是平衡旋钮（见文末「平衡观察」）：防守牌组给防御型更多【防御】，攻击牌组给进攻型、转化型【急救】与【风暴】但少一张【防御】。

### 基础规则术语

| 术语 | 含义 |
|---|---|
| 阶段 | 回合被划分成的固定步骤，顺序为准备 → 判定 → 摸牌 → 出牌 → 弃牌 → 结束 |
| 时机 | 引擎在固定时点执行规则效果的位置，如「回合开始时」「阶段开始时」「阶段结束时」 |
| 威胁 | 攻击造成的结果，记在**承受者**身上；承受者在自己的出牌阶段用【防御】抵消，回合结束时剩余多少威胁就受到多少伤害 |
| 伤害 | 只在回合结束时由威胁结算产生（另有技能自伤／【消耗战】的「失去体力」直接扣血，不算伤害） |
| 摸牌数 | 摸牌阶段摸几张牌，默认 2 张（`draw-count` 通道基准）；先手角色的第一回合 1 张；技能可按通道修正 |
| 手牌上限 | 弃牌阶段结束时允许保留的手牌数，默认等于当前体力值（`hand-limit` 通道是相对体力的偏移，基准 0）。结算顺序是「弃牌 → 回合结束时威胁结算」，所以上限按**结算前**的体力算 |
| 能量 | 使用／打出卡牌要支付的资源，按牌种费用加 `card-cost` 通道修正计（**下限恒为 1**）；**回合开始时回复至上限**（默认 3 点） |
| 上限 | 能量的最大值，由基础值加技能修正得来（【蓄能】+2） |
| 跳过阶段 / 额外的阶段 | 本回合计划中删去某个阶段，或额外插入一个阶段 |

## 对局流程

1. 开局从 4 个代号中随机抽 3 个，玩家选 1 个；AI 从「除玩家所选之外」的代号中随机选 1 个。
2. 双方各自洗一副 20 张的私有牌组，各摸 4 张起手牌；玩家先手，**先手第一回合少摸 1 张**，此后每回合摸 2 张。
3. 回合流程（六阶段）：
   **回合开始时 → 准备阶段 → 判定阶段 → 摸牌阶段 → 出牌阶段 → 弃牌阶段 → 结束阶段 → 回合结束时**。
   - 每个阶段再分为「阶段开始时 → 阶段进行 → 阶段结束时」，规则效果注册在时机上（`rules/phase.ts`）；引擎**先把游标移到下一个子步骤再产生效果**，所以濒死等结算打断流程后不会重复执行同一个子步骤。
   - 摸牌阶段：默认摸 2 张（先手第一回合 1 张）。
   - 出牌阶段：能量在回合开始时已回满，能付几张【打击】就打几张（没有次数限制），每张给对手叠威胁；也可以打出【防御】抵消自己身上的威胁；可发动主动技。
   - 弃牌阶段：手牌上限 = 当前体力值，超出部分须弃置。
   - 回合结束时：先执行时机效果，然后**结算自己的剩余威胁**（等量伤害），再切换回合。这是威胁唯一的兑现点。
   - 准备 / 判定 / 结束阶段本作没有可结算的内容，保留为空阶段与时机扩展点。
   - 阶段可被**跳过**（`skipPhase`）或插入**额外的阶段**（`addExtraPhase`），作用于本回合剩余的阶段计划 `state.phaseQueue`。
4. **单将决胜**：任意一方体力归零即败。

### 结算链（完备性说明）

- **使用【打击】**：合法性校验（牌面选项 + **能量是否够付** + **目标是否合法**）→ 支付能量 → 指定目标 → 按 `threat-per-attack` 通道给目标叠威胁（基础 1，可由技能修正）。
  期间**没有响应窗口**：对手只能在**自己的回合**用【防御】抵消。
- **打出【防御】**：在自己出牌阶段的合法动作，文档 `requires` 要求自己至少有 1 点威胁；支付 1 点能量 → 自己的威胁 −1。
- **威胁结算**：承受者的「回合结束时」把剩余威胁一次性转成伤害（`resolveThreatAtTurnEnd`），先归零再走伤害帧——「受到伤害后」技能（【反击】，人类玩家可选发动）→ 濒死检查。
- **「失去体力」≠「受到伤害」**：技能自伤与【消耗战】直接扣体力，不触发「受到伤害后」技能，但同样会进入濒死；它们也不经过威胁。
- **使用卡牌时的目标选择**：【打击】（唯一候选对手）、【回复】/【防御】（自己）沿用文档缺省目标；
  【急救】声明了 `target`（`scope: any` + 已受伤条件）且候选不唯一时必须由玩家选定，
  界面进入目标选择态后再提交（`Action.use-card.targets`）。可用性与提交共用 `cardTargetChoice`，**按得到就必然提交得成**。
- **多目标**：`TargetSpec.count` 支持 `all`（作用于全部合法候选，如【风暴】同时给双方叠威胁）与 `exactly`（必须选够 N 个）。
  多目标变体的效果写在 `for-each-target` 指令里，逐个把 `target` 绑定为当前目标执行；
  多目标下**不绑定** `target`，直接引用会在加载期被校验器拒绝，避免"以为打了全体却只打了一个"。
- **濒死**：从濒死者开始按座次询问所有存活角色是否使用【回复】；可连续使用直到体力回到 1 以上；队列耗尽则死亡。**濒死中的【回复】不受「仅自己／已受伤／出牌阶段」限制，但仍然要付 2 点能量**。
- **死亡**：标记阵亡 → 弃置全部手牌（进自己的弃牌堆）→ 清空未结算的威胁 → 清空结算栈与处理区（按条目归属分流）→ 判定胜负。
- **牌数守恒**：任意结算步之后，双方**合计 40 张**牌各自恰好属于「自己的牌组／手牌／自己的弃牌堆／处理区」之一，且 uid 全局唯一，引擎在每次 `submit` 后自动校验。
  口径是**全局**而非「每方恒为 20 张」：内容层已没有跨池取牌的牌（历史上的跨池取牌内容已随机制改动退役），但引擎仍按全局口径守护。
- **牌组耗尽**：某方把自己的弃牌堆洗回自己的牌组；两者同时为空则跳过摸牌并记录日志。

结算顺序由显式的**结算帧栈**驱动（`threat → damage → dying → flush`），而不是散落的回调，因此顺序可测、可读、可扩展。

### 对抗机制（占位）

历史上【打击】会开启响应窗口、由目标打出【防御】抵消，这套机制（`contest` / `contest-contribute` 指令、
`respond` 询问、`play-card` 动作、卡牌 `play` 变体、`defend-need-against` 通道）在本作改为威胁机制后**没有任何内容使用**。
它被**刻意保留**为后续反制机制的占位：DSL 词表、引擎结算帧、legality、AI 与界面的响应分支都还在，
`dsl/effect.test.ts` 与 `dsl/channels.test.ts` 用合成内容继续守护它，但内置卡牌一张都不再声明 `play` 变体。
新增反制内容前请先想清楚它与威胁机制的关系，不要照抄旧例。

### 能量系统

| 规则 | 内容 |
|---|---|
| 上限 | 每人 3 点；【蓄能】+2（防御型 5 点） |
| 回复 | **回合开始时回复至上限**，只回复回合角色的能量；没花完的余额留在池中（不超过上限） |
| 付费范围 | **所有使用与打出**：出牌阶段使用【打击】/【防御】/【回复】/【急救】/【风暴】、濒死使用【回复】（救援者付自己的能量） |
| 费用 | 按「**当作的牌面**」计：打击 1 / 防御 1 / 回复 2 |
| 不付费 | 主动技（如【强袭】）、弃牌阶段的弃置、技能的费用弃牌 |

三个直接推论：

1. **防御与进攻争同一份能量**。能量只在自己回合开始时回满，所以「这回合多打两张【打击】」与「先把身上的威胁抵消掉」是一对取舍；留到下回合的能量不会累积，先花掉反而更划算。AI 也按这条行事（先清威胁，再花剩下的能量进攻）。
2. **威胁拖到下回合才兑现，但不会消失**。对手的攻击在你**下个回合结束时**才变成伤害，你有一整个回合决定用多少能量、多少张【防御】去抵消它；扛下伤害则意味着你能把能量全用来进攻。
3. **濒死自救要付 2 点能量**。把能量打光之后被打进濒死，就可能没有能量自救而阵亡；**任何牌面的费用都不允许为 0**（0 费 + 无次数限制 = 无限连击，`energy.test.ts` 对此有断言）。

能量与「牌数守恒」「威胁非负」一样是硬不变式：任意结算步之后必须落在 `0..上限` 之内，引擎在每次 `submit` 后自动校验（`assertEnergyBounds` / `assertThreatBounds`）。

参数在 `src/game/rules/energy.ts`（`BASE_ENERGY_MAX`）与 `src/game/data/dsl/cards/*.json`（每张牌的 `cost`）。

### 消耗战（终止规则，重要）

五张牌的 1v1 存在天然的僵持：【回复】每回合回 1 点，【转换】可以把每张【打击】都当【防御】抵消掉威胁，而牌组会无限洗回，于是对局可以永远打下去。威胁机制也没有消除这种可能：双方只要都愿意把能量花在抵消上，就能一直不受伤。

因此从**第 21 回合**起进入消耗战：每个回合的「回合开始时」时机，回合角色**直接失去体力**（`lose-hp`，不经过威胁、不可被【防御】抵消），每 5 回合递增 1 点（`1 + ⌊(回合 − 21) / 5⌋`）。流失量无上限增长，最终必定超过任何回复能力，保证对局必然结束。能量的回满发生在这条效果之前，所以因消耗战进入濒死的角色是带着满能量被询问的。

消耗战不是写在流程里的特例，而是**一条 DSL 规则文档**：`src/game/data/dsl/rules/attrition.json`
挂在「回合开始时」时机上，与技能触发走同一条通路（`dsl/event.ts` 的时机派发）。
回合数、递增步长、流失量表达式都在文档里；`rules/turn.ts` 只保留 `ATTRITION_TURN` /
`ATTRITION_STEP` / `attritionLoss()` 供界面提示与测试断言，并由 `turn.test.ts` 断言两者一致。

## 代号与技能

四个代号一律以「型」结尾，`name` 直接描述玩法；代号文档不再有 `emoji` 字段，界面与战报只显示代号文本。

| 代号 | 体力 | 技能 | 类型 | 效果 |
|---|---|---|---|---|
| 进攻型 | 4 | **强袭** | 主动 | 出牌阶段限一次：弃置一张手牌，令对方获得 2 点威胁 |
| 反击型 | 4 | **反击** | 触发 | 当你受到伤害后，你可以令伤害来源获得 1 点威胁 |
| 防御型 | 4 | **蓄能** | 常驻 | 你每回合的能量上限 +2（3 → 5） |
| 转化型 | 4 | **转换** | 转化 | 你可以将【打击】当【防御】、【防御】当【打击】使用 |

> 代号 id 分别是 `offensive` / `counter` / `defensive` / `morph`（`transform` 会与 DSL 的转化词表冲突，故用 `morph`）；
> 技能 id 是 `assault` / `riposte` / `charge` / `convert`。

本轮把原本 8 个动物物种精简为 4 个功能代号：代号名与技能名都改成中性的功能词（强袭 / 反击 / 蓄能 / 转换），并去掉形象图标。
被删去的 4 个技能（【疗愈】【威压】【狡黠】【透支】）连同其代号一并退役；
需要「选目标 / 多候选 / 缺省不合格」这类通用机制的测试改用合成内容（见 `skills/active.test.ts`、`stores/game.test.ts`）。

上表的每个技能都是**一份 JSON 文档**（`src/game/data/dsl/skills/*.json`），行为由 DSL 解释器执行；
引擎、合法性判定、AI、界面里**不存在任何技能 id 分支**（由 `dsl/guards.test.ts` 机械守卫）。

转化类技能产生的牌统一用 `VirtualCard` 表示（`as` = 当作什么牌，`source` = 实际消耗的牌，`via` = 技能），威胁、日志、弃牌全部只认它，避免结算与展示分叉。

## 内容 DSL（技能 / 卡牌 / 时机效果）

内容（技能、卡牌、代号、牌组、时机规则、数值常量）全部是 `src/game/data/dsl/**` 下的 JSON，
由 `src/game/dsl/**` 的**单一解释器**执行：

- **词表与类型**：`dsl/kinds.ts`（判别式词表，唯一事实来源）+ `dsl/types.ts`（IR 类型）。
- **加载期校验**：`dsl/validate.ts` 拒绝未知键/未知判别式/悬空引用/死文档/费用 < 1 等，
  一次报出全部问题（`DslLoadError` 带 JSON 路径）；`dsl/registry.ts` 导入即校验。
- **求值**：`dsl/value.ts`（数值表达式 + 修正通道聚合）、`dsl/condition.ts`（条件，可带 `reason` 失败说明）、
  `dsl/target.ts`（目标选取，含多目标 `count` 与 `resolveTargetChoices`）、`dsl/template.ts`（战报模板）。
- **执行**：`dsl/effect.ts` 是全部效果指令的唯一入口（含逐个目标执行的 `for-each-target`），`dsl/internal.ts` 提供牌区/体力/威胁/能量/帧等引擎原语；
  `dsl/event.ts` 把"规则文档 + 技能触发"统一到同一套时机派发上。
- **规范文本**：`docs/dsl.md`（节点全表、语义、扩展指南）；`data/dsl/schema.json` 由
  `dsl/schema.ts` 从词表与字段表**生成**，`schema.test.ts` 逐字节比对防止漂移。

扩展的验收标准：**新增技能 / 卡牌 / 代号只需要加 JSON**，引擎、legality、AI、界面一行都不用改——
`dsl/extensibility.test.ts` 用一个全新主动技、一张全新攻击牌（含牌组与守恒）、一次体力上限调整、**一张使用时选目标的牌**与**一张多目标牌**做了端到端验证。

## 目录结构

```
src/
  game/                    # 规则引擎（纯 TS，不依赖 Vue）
    types.ts               # 领域类型：Card / VirtualCard / Prompt / Action / Frame / Phase / GameState
    engine.ts              # createGame / rollDraft / getPending / submit / advance
    rng.ts                 # mulberry32 + 种子化洗牌（可复现）
    log.ts                 # 中文战报
    testUtils.ts           # 测试用状态构造器
    data/                  # 内容视图：species / cardDefs / deck（实时由 DSL 注册表派生）
      dsl/                 # **内容文档**：ruleset、rules、decks、cards、skills、species + schema.json
    dsl/                   # **DSL 运行时**：kinds/types/validate/registry/value/condition
                           # target/modifier/template/internal/effect/event/schema
    rules/                 # phase（六阶段、时机、跳过/额外阶段）
                           # turn（回合推进、摸牌数、手牌上限、回合结束威胁结算）
                           # threat（叠加／抵消威胁、威胁不变式 —— 基础伤害机制）
                           # energy（能量消耗、回满、能量不变式）
                           # usage（使用次数统计与「每回合限一次」技能记录）
                           # legality / damage / dying / death / cardZones / distance
    skills/                # index（牌面生成、主动技枚举、目标选择解析、通道读取）
    ai/                    # 规则式 AI（按文档派生的用途决策与选目标，确定性）
  stores/game.ts           # reactive 状态 + 目标选择态 + AI 驱动循环（pumpToken 防竞态）
  components/              # StartScreen / DraftScreen / GameBoard / PlayerPanel / HandCard
                           # ActionBar / PromptOverlay / LogPanel / HealthBar
```

引擎以 **prompt 驱动的状态机**对外：`submit(state, action)` 先校验再应用，然后 `advance()` 自动推进系统步骤，直到停在需要人或 AI 决策的点上。回合推进由 `(阶段, 子步骤)` 游标加剩余阶段队列 `phaseQueue` 表示，规则效果按「时机」注册。界面用 `reactive()` 包裹同一份状态对象，引擎就地修改它即自动刷新，引擎本身永远不 import Vue。

## 测试覆盖（316 个用例）

| 文件 | 覆盖内容 |
|---|---|
| `data/deck.test.ts` | 每代号 20 张、三套牌组构成（11/6/3、11/5/2/1/1、10/7/3）、代号→牌组映射、卡牌只有 uid 与牌种、uid 从 uidBase 连续、双方 uid 不重叠、洗牌可复现 |
| `rng.test.ts` | 同种子同序列、分布均匀、洗牌不增删牌 |
| `rules/phase.test.ts` | 六阶段顺序、阶段前后时机、跳过/额外阶段、被濒死打断后不重复执行时机 |
| `rules/turn.test.ts` | 摸牌数与先手补偿、手牌上限与弃牌数、自己的弃牌堆洗回自己的牌组、双耗尽跳过、消耗战递增与时机顺序、回合开始回满能量 |
| `rules/energy.test.ts` | 费用表（1/1/2/2/2）与牌面文案一致、费用恒 ≥ 1、上限与蓄能 +2、开局回满、使用／抵消／回复扣费、转化牌按当作的牌面付费、能量不足拒绝且状态不变、0 能量无可用牌面、技能不耗能、只有回合角色回满 |
| `rules/usage.test.ts` | 按牌名分角色计数、转化牌按当作的牌名计数、技能每回合限一次、回合开始重置 |
| `rules/threat.test.ts` | **威胁机制**：叠加与战报、抵消夹到 0、回合结束结算为等量伤害（体力/`lastDamage` 来源=对手）、无威胁不结算、先抵消后只结算剩余、受到伤害后触发（【反击】）、威胁致死进入濒死、【风暴】自伤在本人回合结束时结算、威胁不变式 |
| `rules/strike.test.ts` | 攻击只叠威胁不掉血、连续使用叠加、能量耗尽后禁用、蓄能多打、【防御】抵消自己 1 点、无威胁拒绝、【防御】只能作用于自己、满血禁用回复、非法动作不改状态 |
| `rules/useTarget.test.ts` | **使用卡牌时的目标**：急救显式目标只治选定的人、不给目标走文档缺省、目标不合法时文档 reason 作为报错且状态不变、风暴 count=all 双方各获得 2 点威胁、只指定一部分目标被拒、无 target 的牌不许带目标、濒死目标必须是濒死者、对称威胁各自在回合结束时结算的终局语义 |
| `rules/damage.test.ts` | 伤害帧原语：扣体力与战报、时机顺序（扣血→【反击】→濒死）、「失去体力」不触发受到伤害后技能也不压伤害帧、失去体力到 0 同样濒死 |
| `rules/dying.test.ts` | 自救、体力为负需连用多张、双方放弃即死亡、救援不受限制、濒死中禁用打击、能量不足无法自救／救援 |
| `rules/death.test.ts` | 弃置全部手牌、未结算威胁清零、处理区按归属收尾、终局后拒绝动作 |
| `skills/transform.test.ts` | 转换双向（都在「使用」语境：打击当防御抵消威胁 / 防御当打击叠威胁）、不能转化【回复】、不可冒用、进攻型不带转化技（【强袭】是主动技）、换文档即新增一条转化 |
| `skills/active.test.ts` | 【强袭】弃牌、令对手获得 2 点威胁、每回合限一次、手牌不足按钮不出现、不消耗能量；目标的文档驱动选择用合成内容验证：`activationTargetChoice` 判定（required / 多候选 / 无 target 不需要选择）、`required:true` 必须显式指定目标 |
| `skills/cardTarget.test.ts` | `cardTargetChoice`：打击唯一候选、回复的候选与 requires 分工、【防御】的出牌阶段变体与「没有濒死变体」、急救候选随受伤情况变化、风暴 all 模式的 size 随候选变化、濒死语境需要濒死者绑定 |
| `skills/passive.test.ts` | 蓄能能量上限 +2、无技能修正时 `threat-per-attack` 基准 1、每个代号恰好一个技能、代号统一以「型」结尾、代号与技能名不重复、代号引用与注册表一一对应、换文档即改数值 |
| `dsl/validate.test.ts` | 40 个校验用例：版本、未知键/判别式/通道/条件/指令、费用下限、牌区与取牌组合、日志占位符与角色、引用完整性与死文档、多目标 count 的字段与冲突、多目标下直接引用 target 被拒、主动技不支持多目标 |
| `dsl/registry.test.ts` | 内置文档全部通过校验、代号与牌种顺序不变、三套牌组构成、`cardRole` / `cardSelfThreat` 结构派生、withRegistry 注入与还原、问题聚合 |
| `dsl/kinds.test.ts` | 词表与阶段表一致、无重复项、字段表覆盖全部判别式 |
| `dsl/value.test.ts` | 全部数值节点、角色数值、通道基准与聚合、修正值为表达式、通道自引用报错 |
| `dsl/channels.test.ts` | 七条通道逐条注入修正并断言引擎行为随之改变（摸牌数、手牌上限、费用与下限 1、攻击范围、能量上限、每张【打击】的威胁）；`defend-need-against` 用合成对抗内容驱动（占位机制不空转）；探针表与 `CHANNELS` 一一对应 |
| `dsl/condition.test.ts` | 全部条件种类、in-processing/card-transformed/picked-count 等运行时绑定 |
| `dsl/target.test.ts` | 急救候选与缺省目标、打击唯一候选、濒死目标、阵亡者不可选、候选为空即报错、文档 reason 作为报错、scope 成员集（过滤前）；**多目标**：all/exactly 的解析、候选不足、重复目标、条件 reason 沿用 |
| `dsl/template.test.ts` | 普通/转化使用、濒死救援、技能日志、vars 优先、未知字段报错 |
| `dsl/effect.test.ts` | 全部效果指令（含 `threat` / `offset-threat`）、四种取牌模式、对抗帧与抵消（占位机制）、濒死脱离、`for-each-target` 逐目标执行与单目标退化、after 延迟语义 |
| `dsl/event.test.ts` | 时机匹配、消耗战规则、触发收集与 when 条件、runTrigger（【反击】）、不可选触发立即执行 |
| `dsl/schema.test.ts` | 字段覆盖率、生成物逐字节一致、每份内容文档过 schema、schema 能拒绝错误 |
| `dsl/guards.test.ts` | 应用代码零内容 id、白名单不过期、不 import node 内置模块、扫描非空跑 |
| `dsl/extensibility.test.ts` | 新主动技 / 新攻击牌（含牌组与守恒）/ 改体力上限 / **使用时选目标的牌** / **多目标牌** 都只改文档 |
| `engine.test.ts` | **200 局 AI 自对局**全终局且牌数守恒、能量与威胁不变式、4×4 代号组合、完全确定性复现、回归：曾经的死循环组合；私有牌组：开局各 20 张且构成正确、摸牌不影响对手、跨池取牌后全局仍守恒（合成技能） |
| `stores/game.test.ts` | 抽将→选将→对局→终局全链路、只提供合法操作、出牌扣能量、能量见底只能结束阶段、弃牌校验、非法操作显示文档 reason、再来一局不被旧回调污染（固定种子）；目标选择器：技能与卡牌两条路径的「按钮可用→进入选择态→候选含文档 reason→选定提交 / 取消不提交」、多目标勾选与数量不足时确认被拦下 |
| `components/render.test.ts` | 界面渲染冒烟：开始游戏→抽将→对局三屏都有内容（视图 Proxy 必须容忍 Vue 的内部键探测，防"只剩背景"回归）、双方威胁点数可见、技能与卡牌的目标选择器、错误说明可见 |
| `ai/index.test.ts` | AI 选目标：自我治疗选自己、`required:true` 会带目标、对敌效果选对手、费用张数由文档决定；卡牌目标：治疗选自己 / 只有对手受伤时选对手、all 模式不传目标；有威胁时先打【防御】抵消；进攻主动技会发动；自伤威胁牌只在能压垮对手时使用 |

（用例数由 `npm test` 汇总；上表按文件列出覆盖点。）

## 已知简化

- **卡牌没有花色与点数**：`Card` 只有 `uid` 与 `kind`，费用与效果都按牌种固定（费用可被 `card-cost` 通道修正，下限恒为 1）。
- **无随机判定**：没有任何需要翻牌判定的机制；唯一的随机来源是洗牌（`rng.ts`），技能只依赖牌种、手牌数与威胁。
- **威胁不跨回合累积**：每个威胁只在「施加者回合 → 承受者回合结束」之间存活一轮，回合结束时无论如何都会清空（兑现为伤害或已被抵消）。因此不存在「攒威胁一波带走」的长线玩法。
- **伤害来源不逐笔追踪**：承受者回合结束结算伤害时，`DamageCtx.source` 直接记 1v1 的唯一对手。若同一回合里既有对手的威胁又有自己【风暴】的威胁，自伤也会被记成「对手造成」——当前内容不依赖这个字段的精确来源。
- **手牌上限按结算前的体力算**：弃牌阶段在「回合结束时威胁结算」之前，所以本回合的威胁伤害不会影响本回合要弃几张牌。
- **能量不跨回合累积**：没有「存能量」的概念，回合开始时只是回复至上限，多出来的部分不会保留；因此留能量过回合没有意义，进攻与抵消都用当回合的能量。
- **技能不消耗能量**：【强袭】以及弃牌都不付能量（主动技的代价写在文档的 `costCards` / 效果里，当前都没有能量费用）。
- **濒死可能因能量不足而救不回来**：濒死【回复】照常收 2 点能量，这是「所有使用与打出都消耗」的直接结果；把能量全花在进攻上，就可能死在回合结束时的威胁结算里。
- **判定 / 准备 / 结束阶段为空**：本作没有需要判定的内容，这三个阶段保留为空阶段与时机扩展点。
- **无额外回合**：阶段层面支持「跳过阶段 / 额外的阶段」，但还没有「额外回合」；将来加入时挂在「回合结束时」的时机上。
- **对抗机制是占位**：`contest` / `contest-contribute` 指令、`respond` 询问、`play-card` 动作、卡牌 `play` 变体与 `defend-need-against` 通道全部保留，但**没有任何内置内容使用**（详见上文「对抗机制（占位）」）；它由合成内容的测试守护，不是活的防御手段。
- **牌池五张牌，没有装备与多目标响应链**：打击 / 防御 / 回复 / 急救 / 风暴；多目标只是「同时作用于全部候选」，没有「多目标 + 逐个响应」的对抗链；加牌仍然只需要一份 JSON（见 `dsl/extensibility.test.ts`）。
- **多目标只支持卡牌的使用变体**：`TargetSpec.count` 出现在主动技的 `target` 上会被校验器以 `bad-combination` 拒绝（主动技仍是单选）；`exactly` 模式必须显式给目标，`all` 模式不接受只给一部分。
- **多目标下不绑定 `target`**：声明了 `count` 的变体，效果里的 `target` 引用必须写在 `for-each-target` 内（加载期守卫），否则会在更深处抛错或只作用于第一个目标。
- **`for-each-target` 的上下文不冒泡**：每次迭代用子上下文，迭代内的 `{amount}` / `{picked}` 不会传给外层效果。
- **对称威胁不再有「同归于尽」**：伤害只在各自的回合结束时结算，而回合是交替的，因此不可能同一时刻双方归零；【风暴】的对称威胁由双方各自在自己的回合结束时兑现（谁先结算谁先可能阵亡）。
- **数值旋钮都在内容文档里**：能量上限、每张【打击】的威胁、摸牌数、手牌上限偏移、费用偏移、攻击范围（`defend-need-against` 只服务占位对抗）七条通道全部由引擎消费（`dsl/channels.test.ts` 守卫），改 `data/dsl/rules/base.json` 的基准值或加一条技能 `modifiers` 就能改数值；引擎里只剩夹取规则（费用恒 ≥ 1、威胁恒 ≥ 0、抵消不溢出）。
- **牌组私有但张数仍按全局守恒**：内容层已经没有跨池取牌的牌，但引擎仍按「全局 40 张、uid 唯一」守护——`engine.test.ts` 用一份合成技能验证「牌易主后归属获得者」这条口径没有退化。
- **三套牌组按代号分化**：`basic`（反击型）、`aggressive`（进攻型、转化型）、`guarded`（防御型）；改牌组只需新增/替换牌组 JSON 并改代号文档的 `deck` 字段。
- **距离恒为 1**：1v1 双方座位距离固定；攻击范围走 `attack-range` 通道（基准 1），`rules/distance.ts` 保留完整接口以便扩展多人。
- **濒死询问顺序**：从濒死者开始按座次询问（1v1 即濒死者 → 对手），`util.ts` 的 `aliveOrderFrom` 是顺序扩展点。
- **【急救】可以治疗对手**：目标选择器列出文档允许的全部候选，玩家可以主动治疗对手（AI 只会治疗自己）。
- **AI 是规则式的**：有威胁时先打【防御】抵消（威胁致命时不保留同类牌），再花剩下的能量进攻；濒死不救对手、弃牌按「攻击→防御→回复」优先级、自伤威胁牌只在能压垮对手时使用。它不会做长线规划（例如不知道「下回合对手可能有几张【防御】」）。
- **AI 不认技能/牌种 id**：用途（治疗、换牌、攻击、防御、回复、进攻主动技）由文档结构派生（`cardRole` / `skillKinds` / `effectsInclude`），策略阈值留在 `ai/index.ts`（`CYCLE_MIN_HP` / `OFFENSE_RESERVE` 等）。
- **内容随应用打包**：`data/dsl/*.json` 由 `import.meta.glob` 静态打包并在启动时校验，**不支持运行时热加载**；注册表与加载是解耦的（`createRegistry` / `withRegistry`），将来要做内容包只需再加一个加载器。
- **可选发动的触发技能只支持「受到伤害后」**：`optional: true` 目前仅允许 `after-damage`（引擎在回合结束的威胁结算里 emit 的事件类时机），其余时机只能是非可选效果。

### 平衡观察

用 200 局 AI 自对局统计（4 代号内容落地后重测；口径与 `engine.test.ts` 的 200 局自对局骨架一致——双方都由 AI 驱动、同种子可复现；【打击】次数按战报中「对 … 使用【打击】/当【打击】」的行数统计）：

| 指标 | 威胁机制（8 物种） | 本次（4 代号） |
|---|---|---|
| 中位局长 | 13 回合 | 15 回合 |
| 平均局长 / 最短 / 最长 | 14.5 / 2 / 29 | 16.5 / 2 / 29 |
| 打到消耗战（≥ 21 回合）的比例 | 39% | 41% |
| 平均每局【打击】次数 | 16.1 | 16.8 |
| 先手胜率 | 50.0% | 56% |
| 代号胜场（200 局） | 狮 40 > 狼 34 > 虎 32 > 熊 31 > 鹿 23 > 豹 19 > 狐 12 > 牛 9 | 反击型 81 > 进攻型 57 > 防御型 37 > 转化型 25 |

观察：

- **反击型领先**（81 胜）：【反击】在伤害兑现时把 1 点威胁还给对手，形成「挨打就反击」的循环，不依赖对手的攻击牌是否留在场上；在只剩 4 个代号、每局都要对攻的池子里收益最稳。
- **进攻型次之**（57 胜）：【强袭】每回合稳定多叠 2 点威胁，但「弃一张手牌」的代价让它无法每回合都开，且会被【防御】抵消。
- **防御型**（37 胜）：【蓄能】让能量上限 3 → 5，但它配的是更偏防守的 `guarded` 牌组，进攻节奏被拉长，整体被反击型克制。
- **转化型垫底**（25 胜）：【转换】要求手里同时有可转的【打击】/【防御】，能量与手牌双重限制下收益最低，是最值得优先补强的旋钮。
- **先手胜率升到 56%**：样本仍是 200 局、口径未变，偏高幅度在噪声范围内，值得用更大样本复核后再决定是否调整先手补偿。

可调旋钮（改完记得同步测试期望值）：

- `src/game/data/dsl/decks/*.json`：三套私有牌组的构成——调高【回复】会拉长对局，调高【防御】会让威胁更容易被抵消。
- `src/game/data/dsl/rules/base.json`：七条通道的基准值，其中 `energy-max`（默认 3）调高会整体加快节奏，`threat-per-attack`（默认 1）调高会整体加重伤害。
- `src/game/data/dsl/cards/*.json`：每张牌的 `cost`（打击/防御 1，回复/急救/风暴 2）与效果——【风暴】的 2 点对称威胁是本次为补偿「不再无视【防御】」而定的数值，过强/过弱都优先改它。
- `src/game/data/dsl/skills/*.json`：技能数值（【蓄能】的 `energy-max` +2、【强袭】的 `costCards` 与 2 点威胁）。
- `src/game/ai/index.ts`：`CYCLE_MIN_HP` / `CYCLE_MAX_HAND` / `HAND_RESERVE` / `OFFENSE_RESERVE` / `HEAL_MAX_HP`——AI 策略不属于内容，仍在代码里（旧的 `DEFENSE_RESERVE` 已随「防御改为自己回合打出」删除）。
- `src/game/data/dsl/species/*.json`：代号体力上限（`maxHp`）、技能表与牌组引用。
- `src/game/data/dsl/rules/attrition.json`：消耗战的起始回合（21）与递增步长（5）；`rules/turn.ts` 的 `ATTRITION_TURN` / `ATTRITION_STEP` 只是文档引用，由测试断言一致。
